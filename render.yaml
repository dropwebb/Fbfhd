services:
  - type: web
    name: web-terminal
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/main.py"
    ports:
      - 5000
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"

